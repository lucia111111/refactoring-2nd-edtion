7.1 레코드 캡슐화하기

- 레코드(Record) : 데이터만 담고 있는 객체, public 필드 혹은 getter/setter없이 필드 직접 접근하는 구조
- 가변 데이터를 저장하는 용도로 객체를 사용
- 사용자는 무엇이 저장된 값이고 무엇이 계산된 값인지 알 필요가 없음
- 레코드를 캡슐화하는 목적 → 변수 자체는 물론 그 내용을 조작하는 방식도 통제하기 위함
